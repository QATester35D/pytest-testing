[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "example_package_shawn"
version = "PACKAGEVERSION"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]